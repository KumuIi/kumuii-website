{
  "name": "kumuii-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "gallery": "node scripts/gen-gallery.mjs",
    "dev": "vite",
    "prebuild": "node scripts/gen-gallery.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "animejs": "^4.3.5",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.5"
  }
}
